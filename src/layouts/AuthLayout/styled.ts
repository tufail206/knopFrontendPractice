import { styled } from '@mui/material';

export const AuthLayoutContainer = styled('div')({
    width: '100%',
    height: '100dvh',
    overflow: 'hidden',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
});